name: "ci-tain"

on:
  push:
    branches:
    - main


jobs:
  test:
    name: Test on node ${{ matrix.node_version }} and ${{ matrix.os }}
    runs-on: ubuntu-latest


    steps:
    - name: test
      run: echo "test"
    
